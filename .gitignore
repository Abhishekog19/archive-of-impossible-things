# Logs
.verification/
.playwright-mcp/
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules/

# Production builds
dist/
dist-ssr/

# Local files
*.local

# Environment variables
.env
.env.*
!.env.example

# Editor files
.vscode/*
!.vscode/extensions.json
.claude/settings.local.json

# Third-party agent skills (installed via `npx skills add`, not project source)
.agents/
.claude/skills/
skills-lock.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Full-resolution visual reference set (~23 MB of PNGs, local only).
# The committed record of the locked target is visual-reference-sheet.jpg —
# downsampled, measurement-safe, referenced by look-target.md.
concept/

# Private project material
# NEVER commit this file — contains hidden portfolio/game source material
personal-source-material.md
